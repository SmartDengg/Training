package com.smartdengg.training;

/**
 * 创建时间:  2016/12/14 17:08 <br>
 * 作者:  SmartDengg <br>
 * 描述:
 */
public class StringTest {

  public static void main(String[] args) {

    //String[] strings = new String[3];
    //strings[0] = "we ";
    //strings[1] = "are ";
    //strings[2] = "family";
    //String string = "see: " + strings[0] + strings[1] + strings[2];
    //String string1 = "see: " + strings[0] + strings[1] + strings[2];
    //string.toString();
    //string1.toString();
    //
    //System.out.printf(string);
    //System.out.printf(string1);
    //
    //String result = null;
    //for (int i = 0, n = strings.length; i < n; i++) {
    //  result += strings[i];
    //}
    //
    //String we = strings[0];
    //String are = strings[1];
    //String family = strings[2];
    //String outputs = we + are + family;
    //
    //System.out.printf(result);
    //System.out.printf(outputs);

    int type = 1;

    switch (type) {
      case 1:
        System.out.println("1");

      case 2:
        System.out.println("2");

      case 3:
        System.out.println("3");
    }

    for (; ; ) ;
  }
}
